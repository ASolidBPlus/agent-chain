{
  "name": "@powerout/chain-svc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Treasury, wallet spawn, name resolution and the chain event tail for Operation PowerOUT",
  "scripts": {
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "abi": "bun run scripts/generate-abi.ts"
  },
  "dependencies": {
    "viem": "2.56.3"
  }
}
